<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.hr.attendance</field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="80"/>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[hasclass('app_settings_block')]" position="inside">

                    <h2>RFID Attendance System RAS2 - thingsintouch</h2>
                    <div class="row mt16 o_settings_container">
                        <div class="col-12 col-lg-6 o_setting_box" title="Set Change Period in the employee detail form (in HR Settings tab).">
                            <div class="o_setting_left_pane">
                                <field name="group_attendance_use_pin"/>
                            </div>
                            <div class="o_setting_right_pane">
                                <span class="o_form_label">Change Period in days</span>
                                <div class="text-muted">
                                    Period in days where it is allowed to change attendance records.Attendance records that are older can not be changed.
                                </div>
                            </div>
                        </div>
                    </div>
            </xpath>
        </field>
    </record> -->

</odoo>
